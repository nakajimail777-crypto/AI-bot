# 運営管理画面（初期版）

入口は `/admin.html`。ユーザー向け画面に管理リンクは追加しません。

## 接続設定

既存の `SUPABASE_URL`、`SUPABASE_PUBLISHABLE_KEY`、`SUPABASE_SECRET_KEY` に加え、サーバー環境変数 `ADMIN_USER_IDS` に管理者本人の Supabase Auth ユーザー UUID を設定します。複数はカンマ区切りです。UUID は Supabase の Authentication > Users で本人のアカウントと照合してください。

未設定の場合、管理 API は閉じたままです。利用量管理の `USAGE_ADMIN_USER_IDS` や本棚の合言葉から会話閲覧権限を自動付与しません。管理者は全会員の保存済み会話を閲覧できます。IDの割り当ては運営者が実施してください。

ユーザー画面で既存のログインを行い、同じオリジンの `/admin.html` を開きます。別タブでログインした場合は「ログイン状態を再確認」を押してください。管理画面側で認証用の新規リダイレクト設定は不要です。ログアウトは同じブラウザーのユーザー画面のセッションにも反映されます。

## 初期版の範囲

- ダッシュボード：本棚の利用可能な資料の正確な件数、直近168時間に更新された会員の会話件数。アーカイブを含み、LINE・お試しの一時会話は含みません。集計期間は日本時間で表示します。
- 本棚：既存の `origin=bookshelf` 資料を25件ずつ表示。取り出し済みも表示し、ダッシュボードの登録件数からは除外。本文は100チャンクずつ取得。登録・差し替え・取り出しは既存の `/rag-register.html` と管理用の合言葉を利用します。
- 学習候補：項目とボタンのみの準備中UI。候補を保存・抽出せず、見本を実件数に数えません。
- 会話：`conversations` を25件ずつ、`messages` を100件ずつ取得。詳細は会話ごとに独立。抽出機能のボタンは無効化し、将来の拡張位置として表示します。

API は GET のみです。各リクエストで Supabase Auth と `ADMIN_USER_IDS` を確認してからサーバーキーで読み取ります。既存RLS・会話保存・ユーザー向けHTML・APIには変更を加えません。秘密キーや会話をログ出力しません。DBマイグレーションは不要です。

## 検証と公開

`node --test tests/admin.test.js` と既存の `npm test` を実行してください。ローカルの `scripts/preview-admin.mjs` は架空データのみの画面検証で、実DB接続確認の代用にはなりません。

公開時は追加ファイルを既存プロジェクトに含め、`ADMIN_USER_IDS` の設定後にデプロイしてください。未ログイン=401、一般会員=403、管理者のみ=200 を実環境でも確認し、本棚内容と会話のページ送りをチェックします。ローカル実装の完了と本番公開は別です。
