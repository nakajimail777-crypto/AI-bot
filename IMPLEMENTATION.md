# スピリットドラゴンAI：使用量・料金表示の実装

2026-09-08。ローカル実装済み。本番デプロイ・本番SQL適用・課金設定変更は未実施。

## 基準とバックアップ

- GitHub `nakajimail777-crypto/AI-bot` の `7528b0a6cde1bb213ffe61fb5ac79f72c864d384` を取得。
- 本番コードのバックアップ：`backups/production-7528b0a.zip`。
- ZIP SHA256：`136F8F0940772E094E603855217F3468F65C745EB28585F3708F60A44E31BAC9`。
- `backups/extracted` に変更前のコピーあり。別の「龍秘術🄬/ai-bot-delete」は変更していない。
- 新しい作業フォルダーは元々コミットのない空リポジトリだったため、変更前保護にはZIPを使用。GitHubへのpushなし。

## 変更ファイル

|ファイル|内容|
|---|---|
|`lib/chat.js`|回答生成・検索の計測、使用量付き保存RPC、保存済み回答の使用量復元、429案内|
|`lib/pricing.js`（新規）|モデル・適用日別単価、思考・キャッシュを含むUSD計算|
|`lib/usage.js`（新規）|実API呼び出しごとの台帳、本文を記録しないメタデータ保存|
|`lib/provider-errors.js`（新規）|日次制限とその他の429分類、確認できた待ち時間の抽出|
|`api/usage.js`（新規）|認証済み利用者の使用量、管理者だけの全体集計|
|`api/bookshelf.js`|資料登録用Embeddingの計測、並列処理の一部失敗時も完了分を記録、429案内|
|`api/import-combination-note.js`|旧ノート登録経路の計測と429案内|
|`scripts/prepare-knowledge.mjs`|Embeddingのエラー種別保持。CLIはDB環境変数があれば準備処理も計測|
|`assets/app.js`|回答別表示、履歴から復元、月間集計、429待機中の入力保持|
|`assets/usage-ui.js`（新規）|使用量の日本語表示、未取得と0円の区別|
|`assets/db.css`、`index.html`|既存画面に折りたたみ詳細とAPI使用量ダイアログを追加|
|`supabase/migrations/20260908_api_usage.sql`（新規）|台帳・回答別使用量・月間集計・権限・保存ラッパー|
|`tests/chat.test.js`、`tests/bookshelf.test.js`|既存テストをRAG・診断ログに合わせ、上限・計測・失敗経路を検証|
|`tests/usage.test.js`、`tests/usage-ui.test.js`、`tests/sql.test.js`（新規）|料金・権限・表示・実PostgreSQLでのSQL検証|
|`scripts/preview-usage.mjs`（新規）|外部通信をしないローカル画面確認用サーバー|
|`package.json`、`pnpm-lock.yaml`|テスト用PGlite 0.3.14を固定、previewコマンド|
|`.gitignore`、`.vercelignore`（新規）|バックアップ・環境変数・確認用ファイルの誤公開を防止|
|`IMPLEMENTATION.md`（本書）|導入・計算・検証手順|

## 維持した動作

- `gemini-3.7-flash:generateContent`、maxOutputTokens=4096。
- `gemini-embedding-2:embedContent`、768次元、RAGしきい値0.58・最大4件。
- 人格文と検索資料の組み込み、直近40件・48,000文字の履歴構築。
- RAG障害時は資料なしで会話継続。
- 元の `chat_save_turn` の本文・所有者・競合チェックは変更しない。
- 本棚の最大50項目、追加・差し替え・取り出し、既存資料登録SQLを維持。
- 既存の非表示・削除・PDF・認証処理は維持。回答別使用量は会話削除と一緒に削除され、料金台帳は残る。

## トークン・料金計算

公式資料：
- https://ai.google.dev/gemini-api/docs/pricing
- https://ai.google.dev/api/generate-content
- https://ai.google.dev/api/embeddings

StandardのテキストAPI。有料換算額をUSDで表示する。税・為替・Vercel/Supabase料金・外部サービスの料金は含めない。

- 入力 = `usageMetadata.promptTokenCount`（履歴・人格文・RAG資料も含む）
- 回答本文の出力 = `candidatesTokenCount`
- 思考 = `thoughtsTokenCount`（省略時0）
- 画面の「出力」= 回答本文 + 思考（内訳も表示）
- 合計 = APIの`totalTokenCount`。文字数から推測しない。
- Embedding入力 = `usageMetadata.promptTokenCount`。返却されなければ未取得。

2026年内のGemini 3.7 Flashの式：

```
生成USD = ((入力 - キャッシュ入力) × 0.75
           + キャッシュ入力 × 0.075
           + (回答本文出力 + 思考) × 3.75) / 1,000,000
Embedding USD = Embedding入力 × 0.20 / 1,000,000
1回答のUSD = その回答の生成USD + その検索のEmbedding USD
```

2027-01-01 UTC以降は公式表の入力1.50・キャッシュ0.15・出力7.50を適用。料金は呼び出し日時・単価バージョンとともに保存し、過去分を新単価で再計算しない。将来の追加変更時は`lib/pricing.js`を更新。

例：入力1,200、回答180、思考80、Embedding入力32なら
生成$0.001875 + Embedding$0.0000064 = **$0.0018814**。
画面は小数6桁、保存は小数12桁。合計は丸める前の値から計算。

### 課金状態

サーバー変数 `GEMINI_BILLING_MODE`：
- `free`：無料枠設定として推定請求額0、有料換算額は別表示。
- `paid`：推定請求額に有料換算額を使用。
- 未設定・その他：課金状態不明。推定請求額は未確定と表示。

GoogleプロジェクトとVercelのキーの対応は未確定なので、無料枠をコードに決め打ちしていない。キー値を出力・転記する必要はない。

## 月間集計と欠損の扱い

- 日本時間の月初00:00から翌月初00:00までをサーバーで集計。
- 回答生成、RAG検索、資料登録を別々に保存。台帳は実際のHTTP呼び出しごとにUUIDを付与。
- 保存済み回答の再表示・同じrequestIdの再送では新しいAPI呼び出しも料金加算も発生しない。
- 保存前の失敗後に実際に再生成した場合は、その費用も累計に含む。並行した別の実呼び出しも計上。
- DB一括保存前にAPI使用量を記録するので、生成後に会話保存が失敗しても取得済み費用は残る。
- 同じ台帳書き込みの再試行は同一イベントIDで重複防止。
- 月間は、画面に残った成功回答だけの合計とは一致しないことがある。
- 429等で使用量が返らない明確な拒否は推定0として分類。通信切断・タイムアウトは不明扱い。
- 使用量未取得を0トークンに変換しない。集計は既知分＋未取得件数。
- 台帳保存失敗は会話を止めず警告ログを出す。欠落した呼び出しをDB自身では数えられないため、画面にも「記録できた呼び出しのみ」と表示。
- 開始前の履歴や、別のツール・同じキーを使う外部アプリの利用は復元できない。
- CLI登録はSupabase用環境変数がなければ従来通り動作し、その費用はこの台帳に入らない。

## 本番導入手順（未実施）

1. GitHub mainと公開コミットの差分を再確認。本作業後に更新があれば統合する。
2. 対象Supabaseへ新規の `20260908_api_usage.sql` **だけを一度適用**する。過去の初期化・登録SQLを再実行しない。
3. Vercel Productionに `USAGE_ADMIN_USER_IDS` を設定。値は管理者のSupabase Auth UUID。複数ならカンマ区切り。メールアドレスや本棚の合言葉ではない。未設定なら全体集計は誰にも公開しない。
4. `GEMINI_BILLING_MODE` を確認済み課金状態に合わせて設定。不明なら未設定のままで有料換算だけ利用できる。
5. 既存のGemini/Supabase/RAG環境変数は維持し、承認後にGitHub・Vercelへ反映。
6. 実ログインで1往復、再読み込み、管理者／非管理者の集計、資料登録、会話削除後も月額が残ることを確認する。

SQL適用前でもチャットは維持するため、新RPC未登録（PGRST202）の場合だけ元の保存RPCへフォールバックする。使用量取得は未準備になる。その他の保存エラーは成功扱いしない。

ロールバックは元のコードへ戻す。追加テーブル・関数は従来動作を変更しないため残してよい。台帳や既存データを削除する手順は含めない。

## 確認方法と結果

Node.js 22以上、`pnpm install --frozen-lockfile` の後：

```
pnpm test
pnpm preview
```

`http://127.0.0.1:4173/` はローカル確認専用。テストユーザー・模擬DB・模擬AIのみ使用し、外部へ会話を送らない。`429`を含む文章を送ると模擬エラーになる。

- 自動テスト42件成功。認証・RAG・再送・429・計測失敗・料金・日付境界・本棚の一部失敗を含む。
- PGlite（実PostgreSQL）で追加SQLを実行：日本時間の月境界、全体／個人集計、未取得件数、会話削除後の費用保持、非管理者権限、二重保存防止、使用量保存失敗でも会話が保存されることを確認。
- DBテストの既存テーブル・保存RPCは確認済み契約を模したフィクスチャ。本番の全スキーマ・同時接続負荷を再現するものではない。
- 実ブラウザーのローカル確認：回答詳細、月間料金、通常送信、429後の入力保持、再読み込み後の復元。1280pxと390pxで既存レイアウトと固定入力欄を確認。コンソール警告・エラーなし。
- 本番の実Gemini応答、実ログイン、本番SQL適用後の動作は公開時に確認が必要。
